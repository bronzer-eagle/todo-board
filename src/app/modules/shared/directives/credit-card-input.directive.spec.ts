import { CreditCardInputDirective } from './credit-card-input.directive';

describe('CreditCardInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCardInputDirective();
    expect(directive).toBeTruthy();
  });
});
